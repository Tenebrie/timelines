## Backups

So something has finally gone wrong, presumably. If not, it's good to have you reading this in advance.

### Overview

The backups are managed by the Chronos service, running Backrest and Restic CLI. Use the `./scripts/backups.sh` script to trigger backup operations.
