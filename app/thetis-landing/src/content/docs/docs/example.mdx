---
title: Example Page
description: A sample page used to verify the Starlight integration renders correctly.
---

import { Aside } from '@astrojs/starlight/components'

This page exists only to confirm the Starlight integration is wired up correctly. Replace it with real content once you're happy with the setup.

## Text and headings

Starlight renders standard Markdown with its own typography, an auto-generated table of contents on the right, and the navigation sidebar on the left.

## Code blocks

```ts
// Syntax highlighting + a copy button come for free.
type World = { id: string; name: string }

function greet(world: World): string {
	return `Welcome to ${world.name}`
}
```

## Asides / callouts

<Aside type="tip">If you can see this styled callout, MDX components are working.</Aside>

<Aside type="caution">This is placeholder documentation — not real product docs yet.</Aside>

You can also write asides with directive syntax instead of the component:

:::note
This note uses the `:::note` directive — handy in plain `.md` files where you can't import components.
:::
